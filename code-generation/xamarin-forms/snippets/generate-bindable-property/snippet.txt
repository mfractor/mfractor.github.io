public static readonly Xamarin.Forms.BindableProperty $name$Property = Xamarin.Forms.BindableProperty.Create(nameof($name$), typeof($type$), typeof($control_type$), default($type$), propertyChanged: On$name$Changed);
public $type$ $name$
{
    get
    {
        return ($type$)GetValue($name$Property);
    }

    set
    {
        SetValue($name$Property, value);
    }
}

static void On$name$Changed (BindableObject bindable, object oldValue, object newValue)
{
    var control = ($control_type$)bindable;
    
    var existing = ($type$)oldValue;
    var incoming = ($type$)newValue;
    
    // TODO: Logic for the property changed here.
}